#!/usr/bin/env bash
# Netlify deployment is disabled. The site is deployed natively on the host server via deploy.sh.
echo "ERROR: Netlify deployments are disabled. Site is published locally via ./deploy.sh" >&2
exit 1
